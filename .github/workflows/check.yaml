# name: reviewdog
# on: [pull_request]
# jobs:
#   infer:
#     name: runner / infer
#     runs-on: ubuntu-latest
#     steps:
#       - name: Check out code
#         uses: actions/checkout@v1
#       - name: Setup Infer
#         uses: srz-zumix/setup-infer@v1
#       - name: Infer
#         run: |
#             sudo apt update
#             sudo apt install libboost-all-dev -y cmake gcc-10 g++-10
#             mkdir build && cd build
#             cmake -DCMAKE_EXPORT_COMPILE_COMMANDS=1 ..
#             make
#             infer capture --compilation-database compile_commands.json --keep-going
#             ls
#       - name: Check Infer report
#         uses: srz-zumix/reviewdog-action-infer@v1
#         with:
#           reporter: github-pr-review # Default is github-pr-check
